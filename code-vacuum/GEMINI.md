# code-vacuum

Professional codebase merger with aggressive junk filtering. Use when the user asks to "vacuum the code", "merge the codebase", or "generate a code payload" for high-context AI analysis.

## Usage
Run: `python3 ${extensionPath}/scripts/payload_merger.py [DIR]`
