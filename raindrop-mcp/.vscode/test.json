{
  "version": "0.1.0",
  "configurations": [
    {
      "name": "Bun Test",
      "type": "bun",
      "request": "launch",
      "program": "${workspaceFolder}/node_modules/.bin/bun",
      "args": ["test"],
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "NODE_ENV": "test"
      }
    }
  ],
  "testPatterns": [
    "tests/**/*.test.ts",
    "tests/**/*.spec.ts",
    "**/*.test.js",
    "**/*.spec.js"
  ],
  "testTimeout": 30000,
  "testEnvironment": "node",
  "tasks": {
    "version": "0.1.0",
    "configurations": [
      {
        "label": "Run semantic-release (dry-run)",
        "type": "shell",
        "command": "bunx semantic-release --dry-run",
        "group": "build"
      }
    ]
  }
}
